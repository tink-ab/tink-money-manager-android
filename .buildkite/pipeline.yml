steps:
  - name: ':male-detective: run detect-secrets on new commits'
    branches: '!master'
    plugins:
    - docker#v3.3.0:
        image: gcr.io/tink-containers/detect-secrets:v1.1.0
        command: ["/opt/detect-secrets"]
        always-pull: true
