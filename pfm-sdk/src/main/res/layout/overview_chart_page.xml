<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools">

  <data>
    <variable
      name="model"
      type="com.tink.pfmsdk.overview.OverviewChartModel"/>
  </data>

  <FrameLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
      android:id="@+id/content"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      tools:ignore="UselessParent">

      <com.tink.pfmsdk.charts.PieChartView
        android:id="@+id/pieChart"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        android:transitionName="@{@string/pie_chart_transition(model.title)}"
        app:thickness_percent="0.3"/>

      <LinearLayout
        android:id="@+id/fadingGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <com.tink.pfmsdk.view.TinkTextView
          android:id="@+id/title"
          style="@style/deci.primary"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:gravity="center"
          android:text="@{model.title}"
          android:transitionName="@{@string/amount_title_transition(model.title)}"/>

        <!-- Do not use wrap_content for width for animation performance reasons-->
        <com.tink.pfmsdk.view.TinkTextView
          android:id="@+id/amount"
          style="@style/peta"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:gravity="center"
          android:text="@{model.amount}"
          android:transitionName="@{@string/amount_transition(model.title)}"/>

        <com.tink.pfmsdk.view.TinkTextView
          android:id="@+id/period"
          style="@style/deci.primary"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:gravity="center"
          android:text="@{model.period}"
          android:transitionName="@{@string/amount_period_transition(model.title)}"/>
      </LinearLayout>
    </FrameLayout>
  </FrameLayout>
</layout>